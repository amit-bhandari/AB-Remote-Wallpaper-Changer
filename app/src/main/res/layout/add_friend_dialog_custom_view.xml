<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginLeft="5dp"
    android:layout_marginRight="5dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/add_friend_user_id"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/add_friend_user_id"
        android:text="@string/add_friend_content"
        android:textSize="15sp" />

    <EditText

        android:id="@+id/add_friend_user_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignStart="@+id/add_friend_progress"
        android:layout_below="@+id/content"
        android:layout_marginStart="20dp"
        android:layout_toStartOf="@+id/add_friend_progress"
        android:ems="10"
        android:inputType="textEmailAddress"
        tools:ignore="LabelFor" />

    <ProgressBar
        android:id="@+id/add_friend_progress"
        style="?android:attr/progressBarStyle"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:layout_below="@+id/content"
        android:layout_marginEnd="11dp"
        android:layout_marginTop="13dp"
        android:visibility="invisible" />
</RelativeLayout>